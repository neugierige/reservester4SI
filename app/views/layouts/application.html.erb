<!DOCTYPE html>
<html>
<head>
  <title>Reservester</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% flash.each do |name, message| %>
	<div><%= message %></div>
<% end %>

<p class="navbar-text pull-right">
	<% if owner_signed_in? %>
		Logged in as <strong><%= current_owner.email%></strong>
		<%= link_to 'Edit profile', edit_owner_registration_path, :class => 'navbar-link' %> | 
		<%= link_to 'Logout', destroy_owner_session_path, method: :delete, :class => 'navbar-link' %> |
		<%= link_to 'Dashboard', dashboard_path %>
	<% else %>
		<%= link_to 'Sign up', new_owner_registration_path, :class => 'navbar-link' %> |
		<%= link_to 'Login', new_owner_session_path, :class => 'navbar-link' %>
	<% end %>
</p>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
